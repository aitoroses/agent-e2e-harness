# Agent E2E Harness examples

Future examples and demo apps live here. Examples must consume public package entrypoints such as `@agent-e2e/harness` and `@agent-e2e/harness/core`; they must not import harness internals from package source paths.

The first real demo app belongs to a later issue. This placeholder exists so issue #2 proves there is an explicit examples/demo home without coupling examples to Harness Core internals.
